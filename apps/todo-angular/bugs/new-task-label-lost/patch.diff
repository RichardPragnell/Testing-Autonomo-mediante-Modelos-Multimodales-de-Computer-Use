diff --git a/src/app/todo-store.ts b/src/app/todo-store.ts
index cbd309f..fd2163f 100644
--- a/src/app/todo-store.ts
+++ b/src/app/todo-store.ts
@@ -14,7 +14,7 @@ export const initialTodos: Todo[] = [
 export function createTodo(text: string): Todo {
   return {
     id: `todo-${Math.random().toString(36).slice(2, 10)}`,
-    text: text.trim(),
+    text: "New task",
     done: false
   };
 }
