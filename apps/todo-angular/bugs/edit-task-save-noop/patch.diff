diff --git a/src/app/todo-store.ts b/src/app/todo-store.ts
index 0000000..1111111 100644
--- a/src/app/todo-store.ts
+++ b/src/app/todo-store.ts
@@ -37,7 +37,7 @@ export function updateTodoText(todos: Todo[], id: string, text: string): Todo[]
     return todos;
   }
 
-  return todos.map((todo) => (todo.id === id ? { ...todo, text: trimmed } : todo));
+  return todos;
 }
 
 export function removeTodo(todos: Todo[], id: string): Todo[] {
