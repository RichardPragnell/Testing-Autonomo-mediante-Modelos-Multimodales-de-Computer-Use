{
  "name": "@agentic-qa/harness-core",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@browserbasehq/stagehand": "^3.2.0",
    "yaml": "^2.6.1",
    "zod": "^4.3.0"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
